"""Browser console for the rig: dataset browsing, collection and sensors.

The modules here hold everything the console does; ``tool/rig_web.py`` is the
entry point that assembles them into one aiohttp application and serves the
static front-end in ``static/``.
"""
