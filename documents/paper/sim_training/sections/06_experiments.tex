\section{Experiments}
\label{sec:experiments}

The experiment asks two questions: do contemporary imitation policies
--- ACT~\citep{zhao2023aloha}, Diffusion
Policy~\citep{chi2023diffusion}, and the vision-language-action model
$\pi_{0.5}$~\citep{pi05} --- learn the two tasks from oracle
demonstrations collected through the teleoperation pipeline; and does
the simple/full mode split localise failures correctly, with simple
mode catching pipeline bugs and full mode measuring generalisation.
This section fixes the protocol; the results subsection is populated
from the long-run report as the living document grows.

\paragraph{Protocol.}
The campaign runs per mode (simple, then full), per task, per policy:
\begin{enumerate}
  \item \textbf{Oracle gate.} Both oracles are dry-run on thirty
    scenarios per task. The experiment aborts if the
    teleoperation-pipeline oracle drops below \SI{95}{\percent} on the
    single task or \SI{70}{\percent} on the relay. The relay bar sits
    below the oracle's measured per-attempt band (roughly
    \SIrange{75}{86}{\percent}) so a thirty-episode sample's noise
    cannot false-abort, yet far above the failure regime of a broken
    oracle.
  \item \textbf{Collection.} Simple mode collects repeated demonstrations
    of a single fixed scenario, which must therefore be one the oracle
    completes at a high per-attempt rate; the relay's per-scenario
    reliability varies, so its fixed scenario is selected by a short seed
    search rather than left at an arbitrary default. Full mode instead
    spreads collection across many training seeds and retries a failed
    seed a bounded number of times, so an occasional hard scenario is
    tolerated. Failed episodes are discarded and only verified successes
    saved.
  \item \textbf{Training.} ACT and Diffusion Policy train from scratch;
    $\pi_{0.5}$ is adapted from its published base with low-rank
    adapters, full finetuning being out of memory reach
    (Section~\ref{sec:diff-tooling}). All training uses the
    LeRobot framework~\citep{cadene2024lerobot}.
  \item \textbf{Checkpoint selection.} Every saved checkpoint rolls out
    on the validation seeds; the best checkpoint per cell (ties to the
    later step) advances.
  \item \textbf{Evaluation.} The selected checkpoint runs once per
    evaluation seed --- thirty held-out scenarios in full mode, thirty
    trials of the fixed scenario in simple mode --- with a video saved
    per episode. Success uses the collection criterion unchanged.
\end{enumerate}

\paragraph{What simple mode measures.}
Simple mode is a weaker instrument than its success rates suggest, and
the collected data say so directly: across every demonstration in a
simple-mode dataset the first recorded state is identical, its spread
zero on every channel. The mode also draws its validation and
evaluation scenarios from the seed it trained on, so a simple-mode
figure reports how completely a policy has fitted one scene and carries
no evidence about generalisation. We therefore read simple mode only as
the pipeline check it was introduced to be, and rely on full mode --- and
on the split-reach relay, whose cube spawn varies by construction --- for
any claim about generalisation.

\paragraph{Results.}
Table~\ref{tab:sim-results} reports the campaign so far. The
overfit-one-scenario cell behaves as its name promises, and we read it only
as the pipeline check Section~\ref{sec:experiments} argues it is. The
split-reach relay is the first cell that asks for generalisation, and both
policies find it hard.

\begin{table}[t]
\centering
\small
\begin{tabular}{llccc}
\toprule
Mode & Task & ACT & Diffusion & $\pi_{0.5}$ \\
\midrule
Overfit-one-scenario & Relay          & --- & 73\% (\SI{37}{\mm}) & --- \\
Full                 & Split-reach relay & 23\% (\SI{93}{\mm}) & 0\% (\SI{347}{\mm}) & --- \\
\bottomrule
\end{tabular}
\caption{Success rate over thirty held-out evaluation scenarios, with mean
placement error in parentheses. Each policy is its validation-selected
checkpoint. The vision-language-action cell is absent because adapting it
exhausted the training machine's memory (Section~\ref{sec:diff-tooling}).}
\label{tab:sim-results}
\end{table}

\paragraph{Checkpoint selection is not a formality.}
We had treated the validation pass as bookkeeping. It is not. Table~\ref{tab:val-curve}
gives the transformer policy's validation success against training step. It
peaks at sixty thousand steps and falls to a fifth of that peak by eighty
thousand, its final checkpoint, while mean placement error more than triples
over the same span. Read off its last checkpoint the policy scores
\SI{13}{\percent} on the evaluation seeds; read off its validation-selected
checkpoint it scores \SI{23}{\percent}. A campaign that skips this step does
not merely lose accuracy, it reports a number that is wrong in a direction it
cannot detect.

\begin{table}[t]
\centering
\small
\begin{tabular}{lcccccccc}
\toprule
Step (thousands) & 10 & 20 & 30 & 40 & 50 & 60 & 70 & 80 \\
\midrule
Validation success & 10\% & 0\% & 10\% & 10\% & 0\% & \textbf{50\%} & 20\% & 10\% \\
Placement error (\si{\mm}) & 246 & 157 & 117 & 49 & 81 & \textbf{43} & 154 & 148 \\
\bottomrule
\end{tabular}
\caption{The transformer policy over-trains on the split-reach relay. Ten
validation scenarios per checkpoint, disjoint from both the training and the
evaluation pools.}
\label{tab:val-curve}
\end{table}

We also record what the selected figure is worth. Selecting the best of eight
checkpoints on ten scenarios is optimistic by construction, and the gap
measures it: \SI{50}{\percent} on the scenarios we selected on against
\SI{23}{\percent} on the thirty we did not. We therefore quote only the
latter, and we would widen the validation pool before drawing any conclusion
that rests on the difference between two nearby checkpoints.

\paragraph{The diffusion policy does not learn this task at all.}
It succeeds on no evaluation scenario, and, unlike the transformer, no
checkpoint rescues it: every one of ten scored zero on validation, and its
mean placement error never fell below a quarter of a metre at any step. So
this is not over-training and there is no checkpoint to select. The rollouts
say what it does instead. It reaches the object and grasps it, and it carries
it to the midline where the hand-off belongs --- the receiving arm's wrist
camera shows the object in its gripper --- and then it releases and abandons
it. It has learned the first half of the relay and nothing about delivery.

\paragraph{What separates the two collection modes.}
The contrast in Table~\ref{tab:sim-results} is not explained by task horizon.
The two datasets are almost the same length per demonstration, a little under
fifty seconds each. Nor is it explained by coverage of the object's starting
region: across the evaluation scenarios, the distance from each starting pose
to its nearest training pose is the same for the successes as for the
failures, so the region is densely sampled and the failures are not the
scenarios that fell in its gaps. What differs is how often each scene is
demonstrated. The overfit-one-scenario dataset shows one scene a hundred
times; the full dataset shows two hundred scenes once each. A single
demonstration of a fifty-second bimanual relay is a thin signal from which to
learn that scene, and this is the strongest evidence we have that the full
mode needs repeated demonstrations per scenario rather than more scenarios.
We have not yet tested that directly, and say so rather than implying the
experiment exists.

\paragraph{A ceiling worth remembering.}
The demonstrator itself completes this task on \SI{87}{\percent} of attempts,
and every one of its failures occurs at placement rather than at the grasp or
the hand-off. No policy trained on its output should be expected to exceed
that, and a policy's placement failures may be inherited rather than its own.
